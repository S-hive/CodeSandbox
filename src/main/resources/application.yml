# 应用服务 WEB 访问端口
server:
  port: 8090

# 用户代码 JVM 安全管理器（Java 21+ 不支持，可设为 false）
sandbox:
  security:
    enabled: true
    # 云托管 fat jar 部署时由 Dockerfile 注入 /app/security-classes
    classpath: ${SANDBOX_SECURITY_CLASSPATH:}
